<% provide :title, t("users.index.all_user") %>
<h1><%= t ".all_user" %></h1>

<%= paginate @users, theme: "twitter-bootstrap-3" %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<%= paginate @users, theme: "twitter-bootstrap-3" %>
